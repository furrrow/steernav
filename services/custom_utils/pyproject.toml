[project]
name = "custom_utils"
version = "0.1.0"
description = "Shared utilities for SteerNav"
dependencies = [
    "custom_utils",
    # Add lightweight shared dependencies here if needed (e.g. "numpy")
]

[tool.uv.sources]
custom_utils = { workspace = true }